
    npm add @kcf-hub/kle-serial

look at example use cases, then write a prompt that specifies in detail what of our data should be exported how. 
it would be interesting to let Claude Opus figure all of that out, but it would probably run into a limit and therefore take longer than doing it by hand.

check if the plate and PCB generators here are already functional enough: https://editor.keyboard-tools.xyz/docs/

